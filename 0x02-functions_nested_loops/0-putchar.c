#include "main.h"

/**
 * main - entry point
 *
 * Description: prints puchar
 *
 * Return: 0 (Success)
 */

int main(void)
{
	char arr[8] = {'_', 'p', 'u', 't', 'c', 'h', 'a', 'r'};
	unsigned int i;

	for (i = 0; i < sizeof(x); i++)
		_putchar(x[i]);
	return (0);
}
