#include <stdio.h>

/**
 * main - Entry Point
 *
 * Description: 'Program should do that'
 *
 * Return: 0 (Success)
 */

int main(void)
{
	for (a = 0; a < b; a++)
		printf("%d\n", a);	
	return (0);
}
